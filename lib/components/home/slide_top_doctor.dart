import 'package:flutter/material.dart';

class SlideTopDoctor extends StatelessWidget {
  const SlideTopDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
